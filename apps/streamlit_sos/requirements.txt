# Streamlit Cloud reads this file (subdirectory requirements next to app.py).
# Pinned to Python 3.12 via runtime.txt at the repo root. Relaxed pins so the
# resolver has flexibility on Cloud's wheel cache; previous hard-pins
# (plotly==6.6.0, pyarrow==23.0.1, numpy==2.4.3) failed silently on Python 3.14.
streamlit>=1.50,<2.0
plotly>=5.24,<7.0
pandas>=2.2,<3.0
pyarrow>=15.0
pycountry>=24.0
numpy>=2.0,<3.0
